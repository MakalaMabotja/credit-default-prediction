# route for returning data profile with options for either description, statistical tests or both
# route for generating basic plots and viewing them

# main purpose of this service is to preprocess data and return clean data with following basic features:
# 1. all date formats correctly formated and converted into individual units (day, month, week, etc)
# 2. return all column into their correct format (i.e numerical - int/float dtype, categorical - object dtype)
# 3. bins for grouping (amount bin, duration bin, etc, as per notebook experiments)

# 